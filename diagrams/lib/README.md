# Diagram Libraries

Store version-pinned, source-readable diagram libraries here when they are needed for repeatable publication builds.

Do not commit third-party binary files unless the author explicitly authorises them.
