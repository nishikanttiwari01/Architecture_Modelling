# Horizon Bank Coverage Summary

`coverage-matrix.csv` is the machine-readable source of truth. This file explains its current baseline without duplicating every row.

The initial rows trace representative deposit, payment and credit coverage across business, process, organisation, data, accounting, control, critical-operation and candidate BIAN mapping records. Blank fields and gaps are deliberate validation and planning inputs. Full-bank coverage remains Phase 1 work.
